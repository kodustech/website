server {
    listen 80;
    listen [::]:80;
    server_name kodus.io www.kodus.io;

    root /home/ezdevs/kodus-files;
    index index.php index.html;

    client_max_body_size 64M;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_read_timeout 120;
    }

    location ~ /\.(?!well-known) { deny all; }

    location ~* \.(jpg|jpeg|png|gif|webp|svg|ico|woff2?|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
        access_log off;
    }

    location ~* \.(css|js)$ {
        add_header Cache-Control "no-cache, must-revalidate";
        etag on;
    }

    location = /robots.txt { allow all; log_not_found off; access_log off; }
    location = /favicon.ico { log_not_found off; access_log off; }

    access_log /var/log/nginx/kodus.io.access.log;
    error_log  /var/log/nginx/kodus.io.error.log;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name kodus.io www.kodus.io;

    ssl_certificate /etc/ssl/kodus-temp/cert.pem;
    ssl_certificate_key /etc/ssl/kodus-temp/key.pem;

    root /home/ezdevs/kodus-files;
    index index.php index.html;

    client_max_body_size 64M;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_read_timeout 120;
    }

    location ~ /\.(?!well-known) { deny all; }

    location ~* \.(jpg|jpeg|png|gif|webp|svg|ico|woff2?|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
        access_log off;
    }

    location ~* \.(css|js)$ {
        add_header Cache-Control "no-cache, must-revalidate";
        etag on;
    }

    location = /robots.txt { allow all; log_not_found off; access_log off; }
    location = /favicon.ico { log_not_found off; access_log off; }

    access_log /var/log/nginx/kodus.io.access.log;
    error_log  /var/log/nginx/kodus.io.error.log;
}
